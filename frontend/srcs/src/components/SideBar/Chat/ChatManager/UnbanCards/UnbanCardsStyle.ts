import styled from 'styled-components';
import { COLORS } from '../../../../../colors';

export const UnbanCardsContainer = styled.div`
    display:flex;
    justify-content: space-between;
    align-items: center;
    padding:4px;
    color: ${COLORS.primary}
`