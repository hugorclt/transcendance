import { ReactElement, ReactNode } from "react"

export type THeptrahedreProps = {
    firstText: string,
    secondText: string,
}

export type THeptraheaderProps = {
    firstText: string,
    secondText: string,
}