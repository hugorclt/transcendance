import styled from "styled-components";

export const HeptaButtonContainer = styled.div`
  display: flex;
  justify-content: center;
  cursor: pointer;
  svg {
    -webkit-filter: drop-shadow(4px 4px 3px rgba(0, 0, 0, 0.7));
    filter: drop-shadow(4px 4px 3px rgba(0, 0, 0, 0.7));
  }
`;
