export enum EPaddle {
  CLASSIC,
  BASIC,
  RED,
  BLUE,
  ORANGE,
  PURPLE,
  GREEN,
}

export enum EMap {
  CLASSIC,
  SPACE,
}

export enum EMode {
  CLASSIC,
  CHAMPIONS,
}

export enum EType {
  BOX,
  SPHERE,
  BOOST,
  SLOW,
  BREAKABLE_BRICK,
  UNBREAKABLE_BRICK,
  WALL,
  GOAL,
  PADDLE,
  CLASSIC_PADDLE,
  BASIC_PADDLE,
  RED_PADDLE,
  BLUE_PADDLE,
  ORANGE_PADDLE,
  PURPLE_PADDLE,
  GREEN_PADDLE,
}

export enum EItem {
  PADDLE,
  TRAIL,
  GOAL,
}
