export enum EPaddle {
  CHAMPIONS,
  CLASSIC,
  BASIC,
  RED,
  BLUE,
}

export enum EField {
  CLASSIC,
  CHAMPIONS,
  BASIC,
}

export enum EMap {
  CLASSIC,
  SPACE,
}

export enum EType {
  BOX,
  SPHERE,
  BOOST,
  SLOW,
  BREAKABLE_BRICK,
  UNBREAKABLE_BRICK,
  WALL,
  GOAL,
  PADDLE,
}
